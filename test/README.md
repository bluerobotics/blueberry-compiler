# Generated binding tests

Run `./test/generate.sh [path/to/file.idl]` to emit bindings into this folder. By default the script uses `crates/parser/tests/fixtures/message_basic.idl`.

To regenerate and run tests in one go, use `./test/run_tests.sh [path/to/file.idl]` (the IDL argument is optional).
