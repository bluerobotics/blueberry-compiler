[project]
name = "blueberry-test"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
    "blueberry-serde @ git+https://github.com/patrickelectric/blueberry-serde-python",
]

[tool.uv]
dev-dependencies = []
